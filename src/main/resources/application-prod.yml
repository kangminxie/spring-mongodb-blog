spring:
  thymeleaf:
    mode: HTML
    cache: false
  data:
    mongodb:
      uri:

logging:
  level:
    root: info
    com.kangmin: debug
  file:
    name: log/app-mongodb-blog-prod.log

application:
  env:
    node: PROD
